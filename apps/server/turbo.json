{
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": ["dist/**", "openapi.yaml"]
    },
    "format": {
      "cache": false
    },
    "test": {
      "cache": true,
      "inputs": ["**/*.go", "go.mod", "go.sum", "testdata/**"]
    }
  }
}
