import { Box } from "@chakra-ui/react";

/**
 * Main resolve UI
 */
export function Resolve() {
  // TODO: TBI
  return (
    <Box boxSize="full" display="flex" alignItems="center" justifyContent="center">
      Resolve
    </Box>
  );
}
